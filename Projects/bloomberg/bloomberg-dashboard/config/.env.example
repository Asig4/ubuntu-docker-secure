# ============================================================
# Bloomberg Dashboard — Variables d'environnement
# ============================================================
# Copier ce fichier : cp .env.example .env
# Puis remplir toutes les valeurs marquées REQUIRED
# Générer les mots de passe : openssl rand -base64 24
# ============================================================

# --- DOMAINE ---
DOMAIN=dashboard.example.com              # REQUIRED: ton domaine (ou IP du VPS)

# --- GRAFANA ---
GRAFANA_ADMIN_USER=admin                  # REQUIRED
GRAFANA_ADMIN_PASSWORD=                   # REQUIRED: openssl rand -base64 24

# --- INFLUXDB ---
INFLUXDB_USERNAME=influx_admin            # REQUIRED
INFLUXDB_PASSWORD=                        # REQUIRED: openssl rand -base64 24
INFLUXDB_TOKEN=                           # REQUIRED: openssl rand -hex 32

# --- POSTGRESQL ---
POSTGRES_USER=bloomberg_user              # REQUIRED
POSTGRES_PASSWORD=                        # REQUIRED: openssl rand -base64 24

# --- JWT (Watchlist API) ---
JWT_SECRET=                               # REQUIRED: openssl rand -hex 32

# --- BINANCE (optionnel, pour données crypto temps réel) ---
BINANCE_API_KEY=
BINANCE_SECRET=

# --- COINGECKO (optionnel, pour market cap & données enrichies) ---
# Gratuit: 30 req/min | Pro: https://www.coingecko.com/en/api/pricing
COINGECKO_API_KEY=

# --- NEWSAPI (optionnel, pour le news feed) ---
# Inscription: https://newsapi.org/register (gratuit: 100 req/jour)
NEWSAPI_KEY=

# --- CRYPTOPANIC (optionnel, pour les news crypto) ---
# Inscription: https://cryptopanic.com/developers/api/
CRYPTOPANIC_TOKEN=

# --- TELEGRAM NOTIFICATIONS (optionnel) ---
# Créer un bot: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- EMAIL SMTP (optionnel) ---
# Brevo (gratuit 300 emails/jour): https://www.brevo.com
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=alerts@example.com

# --- COLLECTE INTERVALS (secondes) ---
COLLECTION_INTERVAL_WS=1                  # WebSocket Binance (1-5s)
COLLECTION_INTERVAL_REST=15               # REST Yahoo Finance (15-60s)
COINGECKO_INTERVAL=60                     # CoinGecko (60-120s)
NEWS_POLLING_INTERVAL=120                 # News (120-300s)

# --- LOGGING ---
LOG_LEVEL=INFO                            # DEBUG, INFO, WARNING, ERROR

# --- TIMEZONE ---
TZ=Europe/Paris
